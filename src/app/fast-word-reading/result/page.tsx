import { FastWordReadingConfiguration } from "../FastWordReadingConfiguration";
import { RunTimeData } from "../RunTimeData";

export default function FastWordReadingResultPage(
  result: RunTimeData,
  configuration: FastWordReadingConfiguration
) {
  return (
    <>
      <ul>
        <li>result</li>
      </ul>
    </>
  );
}
